draft:
  requests:
    types:
      publish-draft:
        allowed-receiver-ref-types:
        - user
        - group
        base-classes:
        - oarepo_requests.types.publish_draft.PublishDraftRequestType
requests:
  types:
    delete-record:
      allowed-receiver-ref-types:
      - user
      - group
      base-classes:
      - oarepo_requests.types.delete_record.DeleteRecordRequestType
    edit-record:
      allowed-receiver-ref-types:
      - user
      - group
      base-classes:
      - oarepo_requests.types.edit_record.EditRecordRequestType
